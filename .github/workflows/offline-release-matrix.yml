name: offline-release-matrix

on:
  workflow_dispatch:
  pull_request:

permissions:
  contents: read

jobs:
  release:
    name: ${{ matrix.os }} / Python ${{ matrix.python-version }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        python-version: ['3.11', '3.12']
    env:
      UV_NO_PROGRESS: '1'
      GIT_CONFIG_COUNT: '1'
      GIT_CONFIG_KEY_0: core.autocrlf
      GIT_CONFIG_VALUE_0: 'false'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v5
        with:
          version: '0.11.15'
          python-version: ${{ matrix.python-version }}
          enable-cache: true
      - name: Install development environment from lock
        shell: bash
        run: |
          uv lock --check
          uv sync --all-extras --frozen
      - name: Enter offline verification boundary
        shell: bash
        run: echo "UV_OFFLINE=1" >> "$GITHUB_ENV"
      - name: Ruff gate
        shell: bash
        run: uv run ruff check src/ tests/ packages/mdrack-core/src/ packages/mdrack-media/src/ packages/mdrack-sqlite/src/
      - name: Mypy gate
        shell: bash
        run: uv run mypy packages/mdrack-core/src/mdrack_core packages/mdrack-sqlite/src/mdrack_sqlite
      - name: Unit and integration gate
        if: runner.os == 'Linux'
        shell: bash
        run: uv run pytest -m 'not e2e and not privacy'
      - name: Windows package contract gate
        if: runner.os == 'Windows'
        shell: bash
        run: uv run pytest tests/unit/test_offline_release_matrix.py tests/unit/test_v13_release_contract.py
      - name: Offline E2E gate
        if: runner.os == 'Linux'
        shell: bash
        run: uv run pytest -m e2e
      - name: Privacy gate
        if: runner.os == 'Linux'
        shell: bash
        run: uv run pytest -m privacy
      - name: Dependency and architecture gates
        shell: bash
        run: |
          uv run python scripts/check_no_forbidden_deps.py
          uv run python scripts/check_core_boundaries.py
          uv run python scripts/check_sqlite_boundaries.py
          uv run python scripts/check_media_boundaries.py
          uv run python -m compileall -q scripts src packages/mdrack-core/src packages/mdrack-media/src packages/mdrack-sqlite/src
      - name: Build and audit all distributions offline
        shell: bash
        run: uv run python scripts/offline_release_matrix.py --output-dir "${{ runner.temp }}/mdrack-release-artifacts" --smoke --allow-index-for-smoke-provisioning
      - name: Documentation and whitespace gates
        if: runner.os == 'Linux'
        shell: bash
        run: |
          uv run python scripts/check_v13_release_packet.py --artifacts-dir "${{ runner.temp }}/mdrack-release-artifacts"
          git diff --check
      - name: Windows artifact evidence gate
        if: runner.os == 'Windows'
        shell: bash
        run: |
          uv run python scripts/check_v13_release_packet.py --skip-quality-replay --artifacts-dir "${{ runner.temp }}/mdrack-release-artifacts"
          git diff --check
      - name: Upload release evidence
        uses: actions/upload-artifact@v4
        with:
          name: release-${{ matrix.os }}-py${{ matrix.python-version }}
          path: ${{ runner.temp }}/mdrack-release-artifacts/
