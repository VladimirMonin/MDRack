# MDRack 1.1 B0 entry gate

Status: baseline and missing application contracts frozen; pending independent B0 review
Evidence level: repository inventory plus local unit/offline verification

## Identity

| Item | Exact value |
|---|---|
| Source baseline | `989aaaeaf029d7bdd2d9704ae578c9a19efe0f94` |
| Branch at B0 start | `master` |
| Active plan | `active-plan-v4.md` workflow artifact, identified by the digest below |
| Active-plan SHA-256 | `f540960507b92a7d790aacae91b363f7884740978d792c6b3071f8b5a9821491` |
| Proposed source plan | `docs/plans/2026-07-20-v1.1-implementation-plan.md` |
| Canonical app owner | `src/mdrack` |
| B0 decision | [ADR-0011](../decisions/0011-v1.1-application-contracts.md) |
| Frozen app contract | [v1.1 application contract](../contracts/v1.1-application-contract.md) |
| Compatibility accounting | [v1.1 compatibility ledger](../compatibility/v1.1-compatibility-ledger.md) |

The baseline is a source revision, not a 1.0 release, branch point, or tag. The
v0.4 packet records final 1.0 as not ready and package contracts as release
candidates. B0 therefore freezes the actual checkout and does not claim that the
proposed plan's assumed 1.0 has completed.

## Current-owner inventory

- Application, CLI, integrations, Markdown ingestion and public engine:
  `src/mdrack`.
- Provider/persistence-neutral contracts and search algorithms:
  `packages/mdrack-core/src/mdrack_core`.
- Provider/filesystem/persistence-neutral timed-media contracts and builders:
  `packages/mdrack-media/src/mdrack_media`.
- Generic clean-catalog persistence/search adapter:
  `packages/mdrack-sqlite/src/mdrack_sqlite`.
- Current prepared-resource import owner:
  `src/mdrack/application/manifest.py`.
- Current explicit clean-catalog facade:
  `src/mdrack/application/resource_catalog.py`.
- Current embedded facade: `src/mdrack/public_api/engine.py` as
  `MDRackEngine`.

No B0 document or test uses `app/src/mdrack` as a repository path.

## Entry conclusion

The existing core/media/SQLite foundations are sufficient inputs for the 1.1
application stages and are preserved rather than replayed. The missing work is
limited to the application/evidence gaps G-01–G-10 in the compatibility ledger.
M1 may begin only after independent review and the planned Git closure for this
exact B0 artifact revision.

No package source, migration, application runtime source, dependency, lockfile,
branch, tag, index, history, or remote was changed by B0.

## Verification record

The B0 implementation worker must attach the exact command results and aggregate
content digest in its terminal handoff. The required gate set is:

```text
uv run pytest
uv run ruff check src/ tests/
uv run ruff check packages/mdrack-core/src/ packages/mdrack-sqlite/src/
uv run mypy packages/mdrack-core/src/mdrack_core packages/mdrack-sqlite/src/mdrack_sqlite
uv run python scripts/check_no_forbidden_deps.py
uv run python scripts/check_core_boundaries.py
uv run python scripts/check_sqlite_boundaries.py
git diff --check
```

The evidence recorded here is local/offline only. A passing suite proves the
checked-out source plus B0 artifacts, not unexecuted external environments.

## Workspace attribution

At B0 start, tracked files were unchanged and `.hermes/` was an unrelated
pre-existing untracked path. B0 does not inspect, stage, alter, or include it.
The six owned B0 paths are the only intended tracked additions.

## Non-claims

- no final 1.0 or 1.1 release;
- no branch, tag, commit, push, package upload, or publication by this card;
- no live LM Studio/OpenRouter/provider call and no network or paid work;
- no private corpus, vault, media source, OCR, Whisper, VLM, visual, acoustic,
  or provider-quality evidence;
- no Windows or Python 3.12 execution;
- no schema migration, destructive cleanup, or cache purge;
- optional OpenRouter and FFmpeg work does not block B0.
