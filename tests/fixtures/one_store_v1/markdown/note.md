# Canonical one-store recovery

A single catalog.sqlite3 stores the Markdown retrieval text for this synthetic fixture.

## Textual similarity

The document, image caption, audio transcript, and video frame captions describe the same canonical one-store recovery topic. Source Markdown must remain byte-for-byte unchanged.
