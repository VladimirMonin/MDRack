[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mdrack-core"
version = "1.0.0rc1"
description = "Provider- and persistence-neutral contracts for MDRack retrieval"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Typing :: Typed",
]
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/mdrack_core"]

[tool.hatch.build.targets.sdist]
include = [
    "/API.md",
    "/CHANGELOG.md",
    "/README.md",
    "/pyproject.toml",
    "/src/mdrack_core",
]
